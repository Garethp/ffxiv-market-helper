import { Badge } from "./Badge";

/** Marks an item priced as high quality. */
export const HighQualityBadge = () => (
  <Badge className="quality-badge" tooltip="Priced as high quality">
    HQ
  </Badge>
);
